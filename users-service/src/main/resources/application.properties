# Application properties
spring.application.name=users
server.port=${USERS_SERVICE_PORT:8080}

# Database properties
spring.datasource.url=${SPRING_MONGODB_URL:mongodb://localhost:27017/user-transactions}
spring.datasource.username=${SPRING_MONGODB_USERNAME:root}
spring.datasource.password=${SPRING_MONGODB_PASSWORD:root}

# RabbitMQ properties
spring.rabbitmq.addresses=${RABBITMQ_HOST:amqp://guest:guest@localhost:5672/}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

broker.queue.transaction.name=${RABBITMQ_TRANSACTION_QUEUE_NAME:default.transaction-queue}

# Flyway properties
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
spring.flyway.table=users_flyway_schema_history

# SpringDoc OpenAPI properties
springdoc.api-docs.version=openapi-3.0
springdoc.swagger-ui.path=/

# Security properties
jwt.public.key=${SECURITY_JWT_PUBLIC_KEY_FILE}
jwt.private.key=${SECURITY_JWT_SECRET_KEY_FILE}